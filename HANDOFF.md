# STM32F103 电机升降项目交接文档

更新日期：2026-09-16

> ## ⚠️ 当前状态：未完成，卡在一个问题上
>
> 「电机完全不转」的根因（速度脚给了静态高电平，超出厂家 98% 占空比上限）已定位并修复。
> 修复后方向逻辑确认无误，**但功率端输出电压幅值不对**：
> 空载实测 10.7 V，预期 23.5 V。
>
> **详见 [第 7 节](#7-当前未解决的问题功率端电压只有-107-v2026-09-16-晚未解决)。**
> 该问题未解决前，不要认为这块板已经调通。

## 1. 项目目标

使用 STM32F103C8T6 开发板控制一块双路 H 桥驱动板（**HC-160A S2**），实现直流电机/推杆升降。

当前要求：**两路同时全速运行，两路方向每 5 秒同步反转一次，上电立即开始，不做软件调速。**

> **关于「不使用 PWM」这条早期要求（重要，已作废）**
>
> 用户最初的措辞是「不要用 PWM」。当时的理解是"速度脚给静态高电平 = 全速"。
> **这个理解被厂家文档推翻**：HC-160A S2 的速度脚 `P` 要的是**真的 PWM 方波**，
> 占空比上限 **98%**。静态高电平 = 100% 占空比，超规格，模块**完全不驱动桥臂**。
>
> 所以固件现在用 TIM1 产生 15 kHz / 98% 的方波。**用户"不要 PWM"的本意是
> "不要软件调速、不要我来调占空比"，这一条依然成立**：占空比是编译期常量，
> 运行时永不改变。区别只是波形由硬件定时器产生，而不是一根恒高的 GPIO。

> 历史：2026-09-16 之前的版本把 B 路禁用（`ENB` 固定低）只是当时调试时临时停用一个电机的
> 做法，不是最终需求，现已恢复为双路同时运行。

仓库位置：`/Users/zachary/Downloads/motor`

主要目录：

- `STM32CubeF1/`：ST 官方 STM32CubeF1，提交 `bb2016e`，HAL/CMSIS 必要子模块已初始化。
- `motor_controller/`：本项目源码、Makefile、链接脚本和构建产物。
- `motor_controller/Src/main.c`：当前应用逻辑。
- `motor_controller/README.md`：用户侧接线与命令说明。
- `motor_controller/jlink_flash.jlink`：J-Link 烧录脚本。

## 2. 硬件

### MCU 开发板

- MCU：STM32F103C8T6，64 KiB Flash、20 KiB RAM。
- 板载 USB Type-C、RESET、BOOT0/BOOT1 跳帽、4 针 SWD。
- 使用 J-Link 通过 SWD 烧录，已经成功识别 Cortex-M3 并烧录校验。
- 正常运行用户 Flash 时：`BOOT0=0`、`BOOT1=0`。

### 电机驱动板：HC-160A S2

2026-09-16 更换。**接口（实物丝印 + 厂家参数图已确认）**：

- **控制排针**（接开发板）：`+`、`-`、`B`、`A`、`a`、`b`、`P`、`P`（共 8 脚）
- **功率排针**（接电源和电机）：`GND(-)`、电机 `A`/`B`、电机 `a`/`b`、`+9V-30V`

厂家原文（参数图 [`docs/hc-160a-s2-vendor-spec.jpg`](docs/hc-160a-s2-vendor-spec.jpg)
上的「驱动模块特点」；这块板没有官方数据手册，该商品详情图是目前唯一的厂家资料）：

> 控制接口非常简单：A.B=0.0时为刹车，A.B=1.0时为正转；A.B=0.1时为反转；**PA为PWM波输入**；
> G控制板共地引脚；**频率最高为60KHZ；占空比最高达98%**；（电机B同样控制）
> 5V.3.3V单片机均可：单电源工作，只需接一个电机电源，和控制板需要共地即可，**不用5V等电源**。

其他标称：导通电阻 0.003 Ω、最大额定电流 60 A、逻辑电源转换芯片宽压输入 9–30 V、
板上有电机 A/B 与 a/b 的转向指示灯。参数图里另有一句
「**3.3V和5V单片机均可以控制本模块**」。

接线图上给两个 `P` 脚的标注是「**15K方波**」，给 `+` 的标注是「**5V(+)输出**」。

### 语义（已确认）

- `A`/`B` 是通道 1 的两个方向输入，`a`/`b` 是通道 2 的。
- 真值表：`A=1,B=0` 正转；`A=0,B=1` 反转；`A=0,B=0` 刹车；**`A=1,B=1` 未定义**。
- 每路的 `P` 脚是**速度输入**，要 PWM 方波，≤60 kHz，**占空比 ≤98%**。

### 实测确认表（2026-09-16，万用表）

| 项 | 结论 |
| --- | --- |
| 控制 `+` | **5V 输出**（模块从电机电源转换），悬空即可 |
| 模块是否需要外部逻辑供电 | **不需要**，单电源工作 |
| 控制输入是否带 5V 上拉 | **没有**。`A`/`B`/`a`/`b`/`P` 悬空时对 `-` 都是 ~0.2 V |
| 3.3V 逻辑能否驱动 | **能**，厂家明确支持 |
| 静态高电平给 `P` 脚 | **不行**，桥臂完全不驱动（电机端恒 0 V） |
| 跳线把 `P` 接 `+`(5V)、`A` 接 `+`、`B` 接 `-` | 仍是刹车态，两个电机端都是 0 V |

> **这条实测推翻了此前文档里 FT 脚的理由。** 原文写「若驱动板自带 5V 上拉，非 FT 脚会被拉到 5V」，
> 但实测**没有上拉**。挪到 FT 脚是**保守选择，不是在修一个已观测到的故障**。
> 结论不变（挪脚没有代价，换一批板子可能就带拉），但理由要说准。

旧板（L298 类，`ENA/ENB/N1..N4`）的实测数据已失效，不要再用于判断。

**仍未实测的**：

1. 两个 `P` 脚哪个配通道 1、哪个配通道 2（固件按 PA8→通道 1、PA11→通道 2 接；
   若接反，对调这两根线即可，不影响安全）。
2. 该板是否用自举电容给高边栅极供电。**风险已大幅降低**：98% 占空比下桥臂每 67 µs
   就开关一次，即使真是自举结构也能被持续刷新。仍建议接电机跑 10 分钟摸 MOSFET 温度。

## 3. 当前引脚分配

| STM32 | 驱动板/外设 | 当前配置 |
| --- | --- | --- |
| PA8 | `P`（通道 1 速度） | **AF 推挽，TIM1_CH1，15 kHz 方波，98% 占空比** |
| PA11 | `P`（通道 2 速度） | **AF 推挽，TIM1_CH4，15 kHz 方波，98% 占空比** |
| PB8 | `A`（通道 1 方向） | 推挽输出，每 5 秒翻转 |
| PB9 | `B`（通道 1 方向） | 推挽输出，每 5 秒翻转 |
| PB6 | `a`（通道 2 方向） | 推挽输出，每 5 秒翻转 |
| PB7 | `b`（通道 2 方向） | 推挽输出，每 5 秒翻转 |
| PB10 | 下限位 | 内部上拉，低电平触发 |
| PB11 | 上限位 | 内部上拉，低电平触发 |
| PB12 | UP 按钮 | 内部上拉，低电平触发 |
| PB13 | DOWN 按钮 | 内部上拉，低电平触发 |
| PA9/PA10 | USART1 TX/RX | 115200 8N1 |

**只有表中前六行是接 HC-160A S2 控制排针的。** PB10–PB13 的限位/按钮和
PA9/PA10 的串口是 STM32 自己的外设，驱动板上没有对应端子，直接接在 MCU 上；
四路开关都用内部上拉，每个只要两根线（信号 + GND）。**这四个脚全部悬空不接，
固件也能正常运行**（恒为"未触发/未按下"），只是失去行程保护、手动按钮和串口。

**六个信号脚全部选在带 FT（5V 容忍）的引脚上，是保守选择。**
STM32F103 上 PA0–PA7、PB0、PB1、PC13–PC15 **不带 FT 标记**，输入绝对最大额定
`VDD+0.3V ≈ 3.6V`；从上电复位到 `GPIO_Init()` 跑完有 **2–3 ms**，这期间所有 GPIO 都是
浮空输入。**实测本板控制输入没有上拉**，所以非 FT 脚本来也没事——但保留 FT 选择，
因为代价为零，而换一块板可能就有上拉。（旧版用的 PA0/PA1/PB0/PB1 全是非 FT 脚。）

顺带好处：PA8/PA11 正是 TIM1_CH1/CH4，做 PWM 不需要额外挪线。

所有系统必须共地：STM32 GND、驱动控制 `-`、功率电源负极。
电机大电流回路直接走功率侧 `GND(-)`，不要过控制排针的细线。

## 4. 当前固件行为

- HSI/PLL，系统时钟 64 MHz。
- **TIM1 产生两路 PWM**（CH1→PA8、CH4→PA11）：15 kHz、98% 占空比，全程自由运行，
  运行时不重新配置。占空比是编译期常量 `PWM_DUTY_PERCENT`。
- TIM3 产生 1 kHz 中断，作为毫秒定时基准，与 PWM 无关。
- 上电进入 `auto_mode`，`auto_direction = AUTO_START_DIRECTION`（默认正方向）。
- 每 5000 ms 翻转一次方向，**两路同步**翻转。
- 方向脚用**单次 `GPIOB->BSRR` 写入**成对更新（`dir_write()`），
  避免"反转切正转"时出现 `A=1,B=1` 这个真值表未定义的中间态。
- 上/下限位对两路共用。对应方向限位触发时该路刹车。
- `motor_stop_a()` / `motor_stop_b()` 写 `A=B=0`，在 HC-160A S2 上是**刹车**，
  **不是断电**：速度脚仍在输出 98% 方波，桥臂一直带电。这是升降机构更安全的选择，
  但被负载反拖时会发热，不能替代机械限位。
- 反转默认**没有**停机延时，直接从一个方向切到另一个方向。
  留了编译期开关 `REVERSE_DEAD_TIME_MS`（默认 `0U`）可随时启用。
- 启动时通过串口打印复位原因（`print_reset_cause()`），并打印当前 PWM 设置。
  注意 STM32F103 **没有** BOR 掉电复位标志位（那是 F2/F4 的），
  供电跌落表现为 `PORRSTF`，代码里打印为 `RESET: POR/BROWNOUT`。

编译期开关：

| 宏 | 默认 | 作用 |
| --- | --- | --- |
| `CHANNEL_B_ENABLED` | `1` | 置 `0` 恢复单电机调试（TIM1_CH4 占空比设 0，`a`/`b` 停在刹车） |
| `REVERSE_DEAD_TIME_MS` | `0U` | 换向前刹车毫秒数；`0` = 直接反向 |
| `AUTO_START_DIRECTION` | `1` | `1` = 上电先正转，`-1` = 先反转 |
| `PWM_FREQ_HZ` | `15000U` | 速度脚方波频率；厂家上限 60 kHz |
| `PWM_DUTY_PERCENT` | `98U` | 速度脚占空比；**厂家上限就是 98，不要改大** |

`PWM_ARR` / `PWM_CCR` 由上面两个宏推导，不用手算：当前是 `ARR=4265`（→15.0 kHz）、
`CCR=4180`（→97.98%）。

串口命令仍存在：

- `r`：重新进入 5 秒自动反转（同时复位刹车状态机）。
- `s`：刹车并退出自动模式。
- `u` / `d`：持续正/反方向。
- `A+5000`、`B-3000`、`X+8000`：单路/双路定时方向命令，单位 ms。
- `A0`、`B0`、`X0`：刹车。
- 早期版本的 `Pxx` 和单个数字命令（遗留 PWM 占空比）**已删除**。

## 5. 编译与烧录

官方工具链已经安装：

```text
/Applications/ArmGNUToolchain/15.3.rel1/arm-none-eabi
```

注意：`/opt/homebrew/bin/arm-none-eabi-gcc` 是 Homebrew 的 compiler-only 版本，按
`--without-headers` 构建，缺少 newlib；不要使用。Makefile 会优先自动找到 `/Applications` 下的官方工具链。

构建：

```sh
cd /Users/zachary/Downloads/motor
make -C motor_controller clean
make -C motor_controller
```

当前版本（TIM1 PWM）已成功构建，`-Wall -Wextra` 下无告警，
`text 7116 / data 32 / bss 1808`。链接阶段会出现 `_close`/`_read`/`_write`/`_lseek`
not implemented 和 RWX segment 两类提示，来自 `--specs=nosys.specs`，属既有现象。
产物：

- `motor_controller/motor_controller.elf`
- `motor_controller/motor_controller.bin`

J-Link 烧录：

```sh
make -C motor_controller flash PROGRAMMER=jlink
```

J-Link 接线：SWDIO→PA13、SWCLK→PA14、GND→GND、VTref→3.3V，NRST 建议连接。
用户实际烧录日志已确认：J-Link ARM-OB V7、VTref 3.300 V、找到 Cortex-M3、下载成功、
`Verify successful`、复位并运行。

> 2026-09-16 晚最后一次尝试烧录时 J-Link 未连接（`Cannot connect to the probe/programmer`），
> 用户说明「硬件方面我来操作即可」。**PWM 版本的固件已经编译好但尚未烧录验证。**

## 6. 已知问题与安全风险

1. **`P` 脚必须收到 ≤98% 占空比的方波。** 给静态高电平（100%）模块完全不驱动桥臂。
   **绝不要为了"更保险"把速度脚改成静态高电平或把 `PWM_DUTY_PERCENT` 改成 100。**
2. **`+9V-30V` 是电机功率电源，绝不能接到 STM32。** Blue Pill 板载是 5V→3.3V LDO，
   9V 灌进去直接烧。
3. **绝不把控制排针的 `+` 接进 STM32 的 5V 或 3.3V。** 实测它是 5V **输出**，
   会和 Blue Pill 的稳压器对打。
4. **两路全速且上电自动启动，没有急停。** 接机械负载前必须确认硬件限位有效；
   首次调试建议拔掉电机线。
5. **换向默认无死区，且两路同时换向。** 冲击电流 ≈ 2 倍堵转再翻倍，
   可能拉跌供电甚至导致 MCU 复位 → 重启后又换向 → 自锁循环。
   判据是串口反复打印 `RESET: POR/BROWNOUT`；补救是把 `REVERSE_DEAD_TIME_MS` 改成 `200U`。
6. **限位共用 PB10/PB11。** 两个推杆驱动同一机构时共用是正确的；
   若两路行程不同，必须各自独立限位。
7. **`s`/`X0`/`A0` 是刹车不是断电。** 速度脚持续输出，桥臂一直带电，被反拖时会发热。
8. **Type-C 5V 引脚曾被测为约 3.3 V。** 后续已使用 J-Link，开发板供电方式仍需明确；
   J-Link VTref 通常是参考输入，不应假定它能给目标板供电。

## 7. 当前未解决的问题：功率端电压只有 10.7 V（2026-09-16 晚，**未解决**）

这是**目前唯一卡住的问题**。PWM 改造已经生效，但输出电压幅值不对。

### 现象

条件：**电机未接**（空载），功率电源 220V→24V / 8.3 A 转换器接在 `+9V-30V`。

实测四个功率端子 `A`、`B`、`a`、`b` 对 `GND(-)` 的电压：
**在 10.7 V 和 0 V 之间跳动，周期 5 秒**，与固件的自动换向周期一致。

### 已经确认没问题的部分（好消息）

四个端子**跟着 5 秒周期正确地分成两组换边**，即 `A`/`a` 一组、`B`/`b` 一组，
两组相位相反。这说明下面这些**全部正常**：

- 方向译码正确（`A=1,B=0` / `A=0,B=1` 被模块正确识别）
- 两路通道同步换向
- 换向时序、`dir_write()` 的原子写入

> **一个尚未确认的前提：新固件到底烧进去了没有？**
> 换向跳变这个现象**在修复前就有**（当时读数是 ±1.96 V 来回跳），
> 所以"会换边"本身不能证明 PWM 版固件已经在片子上跑。
> 唯一的变化是幅值从约 2 V 变成了 10.7 V，但那也可能是测量条件变了导致的。
>
> **烧录后用万用表量 PA8/PA11 对 GND 即可确定**：
> 读到 **≈3.2 V** = PWM 版在跑（15 kHz 方波的平均值）；
> 读到 **3.3 V 恒定** = 还是旧的恒高固件。
> 这一条没确认之前，下面"PWM 被模块接受"之类的结论都不成立。

### 异常的部分

**幅值应该是 `0.98 × 24 ≈ 23.5 V`，实测只有 10.7 V。**

注意 10.7 V 对应的是"高边导通时"的电平。修复前同一个端子在这个相位读的是
**约 2 V**（纯漏电），所以 10.7 V 说明高边**确实开始导通了**——方向是对的，
只是导通得不充分。

> **空载读数只能定性、不能定量。** H 桥输出空载时没有电流通路，节点悬空，
> 电位由模块内部漏电、转向指示灯和万用表 10 MΩ 输入共同决定。
> "有没有在切换"可信，"能输出多少伏"不可信。**定量必须带载测。**

### 首要怀疑：高边自举电容在 98% 占空比下充不满

线索是**厂家为什么专门标"占空比最高 98%"**：

- 若高边用**电荷泵**驱动，100% 占空比也没问题，不需要这个上限
- **专门卡在 98%，恰恰是自举电容（bootstrap）驱动的特征** ——
  它必须靠低边管导通的那段时间给电容充电

而当前 98% 占空比 + 15 kHz，留给自举电容的充电窗口只有：

```
(1 - 0.98) / 15000 = 1.33 µs
```

如果电容充不满，高边栅极电压不足，MOSFET 落在放大区 ——
表现就是输出只有十几伏、带不动负载，与 10.7 V 的现象吻合。

**验证方法是一行改动：把频率降到 1 kHz。**

| `PWM_FREQ_HZ` | 关断窗口 |
| --- | --- |
| `15000U`（当前） | 1.33 µs |
| `1000U`（待验证） | **20 µs**（15 倍） |

占空比不变，如果降到 1 kHz 后带载电压升到 23.5 V，即可确认是自举问题。

### 下一步要做的（按顺序）

0. **先确认 PWM 版固件真的烧进去了**：量 PA8 和 PA11 对 GND，
   **≈3.2 V = 新版在跑**，3.3 V 恒定 = 还是旧的恒高固件。
   这一步没做，后面的数据全部无从解释。
1. **量电源本身**：`+9V-30V` 对 `GND(-)`。8.3 A 的转换器空载应该稳稳是 24 V。
   不是 24 V → 问题在电源，不在模块。
2. **接上负载再量**（最好是电机，接功率排针 `A`/`B`；或 24 V 汽车灯泡、几十欧功率电阻）。
   万用表跨在 `A`、`B` 之间：
   - ≈23.5 V 且 5 秒换向时极性翻转 → **问题解决**
   - 仍 ≈11 V 或一接就塌 → 做第 3 步
3. **把 `PWM_FREQ_HZ` 改成 `1000U` 重新编译烧录**，重复第 2 步。
   电压起来了 → 自举问题确认，据此定最终配置。
4. **有示波器的话优先用示波器**，直接看功率端和 PA8/PA11 的波形，
   一步就能分清"高边真的只有 11 V"还是"万用表平均值算错了"。

### 其他可能性（次要）

- **万用表无法正确平均 15 kHz 方波。** 老式/廉价 DMM 的 DC 档对高频方波
  可能给出无意义的值。示波器可排除。
- **两个 `P` 脚接反。** 不影响幅值，只会让两路速度信号互换；对调 PA8/PA11 试。
- **电源带载跌落。** 当前空载，可能性低，但第 1 步顺手就能排除。

## 8. 建议下一步

按优先级执行：

1. **先解决第 7 节的 10.7 V 问题**，那是在接电机之前必须搞清楚的。
2. **烧录 PWM 版固件后，用万用表确认 PA8/PA11 上有方波**。
   普通万用表 DC 档应读到约 `0.98 × 3.3 ≈ 3.2 V`（方波平均值）；
   有示波器直接看波形最直接。
3. 接空载电机，限流 1–2 A，确认两路都转、且每 5 秒同步换向。
   若某个电机转向相反 → 对调该电机的两根输出线（双推杆镜像安装时常见）。
4. 观察是否出现 `RESET: POR/BROWNOUT`。出现就把 `REVERSE_DEAD_TIME_MS` 改成 `200U`。
5. 单独把 `REVERSE_DEAD_TIME_MS` 临时改成 `200U` 烧一次，确认死区开关本身工作，再改回 `0U`。
6. 最后才接推杆/机械负载，并做 10 分钟满功率温度测试（摸 MOSFET 和栅极驱动芯片）。

## 9. 给接手 Agent 的关键判断

**「电机完全不转」的根因已经找到并修复，不要再从软件侧重新推理一遍。**

根因：HC-160A S2 的速度脚 `P` 是 **PWM 输入，占空比上限 98%**；
旧固件给的是**静态高电平 = 100% 占空比**，超规格，模块拒绝驱动桥臂。
方向译码一直是对的（实测低边管跟着 5 秒周期反相），电平也够（厂家支持 3.3V）。

现在速度脚由 **TIM1_CH1/CH4** 输出 15 kHz / 98% 方波。

**但截至 2026-09-16 晚，问题只解决了一半**：空载实测功率端已能按 5 秒周期
正确换边（方向逻辑确认无误），**但幅值只有 10.7 V，预期是 23.5 V**。
**先看第 7 节**，那是当前唯一卡住的问题。

**如果烧录后电机仍不转，按这个顺序查，不要再改 MCU 代码：**

1. PA8/PA11 上**有没有方波**（示波器最佳；万用表 DC 档应读到 ~3.2 V，不是 3.3 V 也不是 0 V）。
   没有波形 → 查 TIM1 是否真的启动（`HAL_TIM_PWM_Start` 对 TIM1 还会置 MOE 位）。
2. 控制排针是否**共地**（STM32 GND ↔ 控制 `-` ↔ 功率电源负极）。
3. 功率电源是否真的加在 `+9V-30V`，电压是否在 9–30 V 内。
4. 两个 `P` 脚是否接反（对调 PA8/PA11 两根线试）。
5. 电机是否接在**功率排针**的 `A`/`B`（或 `a`/`b`）上，而不是控制排针。

**万用表测电机端电压要注意**：DC 档读的是**平均值**，PWM 输出在低占空比下也会读出低电压。
判断"高边有没有开"不能只看一个数字，要看它跟不跟 5 秒换向周期同步变化。
